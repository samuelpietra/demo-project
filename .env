# CONFIGURATION VARIABLES ONLY!!!
# NO SECRETS IN THIS FILE
# DON'T CHANGE THESE VALUES
# OVERRIDE THEM IN .env.local if necessary

PORT=3902

# Overridden in .env.local — the app targets PostgreSQL (see README > Environment)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/demo_project